/vendor/
/.php-cs-fixer.cache
/.phpunit.cache/
/composer.lock

# Auto-generated by FrameworkBundle's PhpConfigReferenceDumpPass whenever a debug-mode kernel
# boots with this repo as its project dir (as the bundle's own test kernel does) — IDE
# autocompletion helper only ("for apps only. Bundles SHOULD NOT rely on its content" per the
# file's own header). Never hand-edited, never read by anything in this repo.
/config/reference.php
